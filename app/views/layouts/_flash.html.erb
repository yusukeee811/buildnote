<div class="flash">
  <% flash.each do |key, value| %>
    <% key = "secondary" if key == "notice" %>
    <% key = "danger" if key == "alert" %>
    <div class="alert alert-<%= key %> mb-0"><%= value %></div>
  <% end %>
</div>
